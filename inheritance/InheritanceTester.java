package inheritance;
// constructor is a special method that is used to initialize objects in java
// constructor has the same name as the class
//constructor does not have a return type
// constructor is called when an object of the class is created
// in inheitance the constructor of the parent class is called first
// if the parent class does not have a default constructor, the child class must explicitly call the parent constructor

public class InheritanceTester {
    public static void main(String[] args) {
        //Person person = new Person();
        Employee employee = new Employee();
        
    }
    
}
